name: python3_devel
channels:
  - conda-forge
dependencies:
  - conda-pack
  - kafka-python
  - pip
  - python=3.10
  - pip:
    - slac-alarm-manager
#
# Stuff that isn't on PyPI or GitHub that is manually installed in prod
# epics-batch-get
# gauge-widget
# lcls-parameters
# logicgate
# matlog
# model
# physicselog
# wmctrl
